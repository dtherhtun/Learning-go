package main

import "fmt"

func main() {
	// the variable declaration below is invalid due to invalid // naming convention
	var first = "DTher"
	var address = "404 street"
	var yob = 1994
	fmt.Println(first, address, yob)
}
